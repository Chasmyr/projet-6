const test = document.querySelector('.test')

function sortMedia(medias) {
    // faire 3 fonctions de tri ? ou alors un objet en entrée
    // pusi trier par nbr de like
    // tri par orde alphabétique
    // tri par date
    console.log(medias)
}

test.addEventListener('click', () => {
    sortMedia(mediasData)
})